<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    <style>
        {{{commonCss}}}

        .statement-body {
            margin-top: 20px;
            text-align: justify;
        }

        .info-grid {
            border: 1px solid var(--border-color);
            padding: 15px;
            margin-bottom: 20px;
        }
    </style>
</head>

<body>
    <div class="container">
        <div class="header">
            <img src="{{logoPath}}" alt="Logo">
            <h1>महाराष्ट्र शासन</h1>
            <h2>कार्यकारी दंडाधिकारी तथा उपविभागीय दंडाधिकारी यांचा न्यायालय, {{policeStationName}}</h2>
        </div>

        <div class="text-center bold" style="font-size: 14pt;">साक्षीदाराचा जबाब</div>

        <div class="info-grid">
            <p><span class="bold">नाव:</span> {{witness.name}}</p>
            <p><span class="bold">वय:</span> {{witness.age}} वर्षे</p>
            <p><span class="bold">पत्ता:</span> {{witness.address}}</p>
            <p><span class="bold">व्यवसाय:</span> {{witness.occupation}}</p>
        </div>

        <div class="statement-body">
            <p>
                मी, <span class="bold">{{witness.name}}</span>, प्रतिज्ञापूर्वक खालील जबाब देत आहे:
            </p>
            <p class="mt-20">{{statement}}</p>
        </div>

        <div class="footer mt-20">
            <div class="signature-box">
                {{#if witness.signature}}
                <img src="{{witness.signature}}" alt="Signature">
                {{/if}}
                <p>साक्षीदार</p>
            </div>
            <div class="signature-box">
                <p class="mt-20">कार्यकारी दंडाधिकारी</p>
            </div>
        </div>
    </div>
</body>

</html>
